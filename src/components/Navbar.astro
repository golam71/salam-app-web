---
import { Icon } from "astro-icon";
import IconButton from "../elements/IconButton.astro";

// export interface Props {
//   title: string;
// }

// const { title } = Astro.props as Props;
---

<nav
  class="relative w-full flex flex-wrap items-center justify-between py-2 bg-smoke-400 hover:text-gray-700 focus:text-gray-700 shadow-lg"
>
  <div
    class="container-fluid w-full flex flex-wrap items-center justify-between px-6"
  >
    <div class="left container-fluid">
      <a
        class="flex items-center text-gray-900 hover:text-gray-900 focus:text-gray-900 mt-2 lg:mt-0 mr-1"
        href="#"
      >
        <Icon name="bi:moon-stars-fill" height="30px" class="text-swans-50" />
      </a>
    </div>
    <div class="right container-fluid">
      <IconButton
        ><p slot="text">Get Salam App</p>
        <div slot="icon" class="flex">
          <Icon name="file-icons:apple" height="20px" />

          <Icon name="uil:android" height="20px" />
        </div>
      </IconButton>
    </div>
  </div>
</nav>
